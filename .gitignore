# Only keep data-v1 in this repository.
*

# Keep git ignore rules tracked.
!.gitignore

# Allow data-v1 and all its contents.
!data-v1/
!data-v1/**

# But keep raw source local-only.
data-v1/source/
data-v1/source/**
